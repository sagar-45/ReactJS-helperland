import React from 'react'

export default function WH_reason(props) {
    return (
        <>
            <div className="reson">
                <p>{props.title}</p>
                <p>{props.text}</p>
            </div>
        </>
    )
}
